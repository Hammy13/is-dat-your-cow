# Source Layout

The executable prototype modules currently live at the root of `cow_identity_prototype/` for simple `python -m` execution.

This `src/` folder is reserved for a future package split if the prototype is converted into a distributable library.
