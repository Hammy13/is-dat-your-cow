# Models

This folder is reserved for local trained weights or exported checkpoints.

The current prototype uses:

- `yolo11n.pt` from the repository root for detection
- torchvision pretrained `ResNet50` or `ViT` at runtime for embeddings
