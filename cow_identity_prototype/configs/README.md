# Configs

Use `default_config.json` as the main runtime configuration file.

You can override:

- YOLO thresholds
- mesh-grid size
- embedding backbone
- matching thresholds
- clustering settings
- input/output paths
